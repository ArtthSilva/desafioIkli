import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    story: {
        alignItems: 'center',
        gap: 10,
    },
    container: {
        borderWidth: 4,
        borderColor: '#f7b55a',
        justifyContent: 'center',
        alignItems: 'center',
        marginLeft: 12,        
    },
    image: {
        resizeMode: 'cover',
    },
});